import { Callout } from 'nextra/components'

### Embed interaction
```ts
// Set player volume level
embed.setVolume(0.45);

// Start playing
embed.play();

// Get player stats data
playbackStats = embed.getPlaybackStats();
```

<Callout type="warning" emoji="⚠️">
  Functions list is still under development.
</Callout>